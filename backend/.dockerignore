.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.env
*.md
# pyproject.toml declares `readme = "README.md"`, and hatchling validates
# that file exists while generating metadata — without this exception
# `pip install .` fails the image build with
# "OSError: Readme file does not exist: README.md".
!README.md
